\documentclass[dvipsnames]{beamer}

\addtobeamertemplate{navigation symbols}{}{%
    \usebeamerfont{footline}%
    \usebeamercolor[fg]{footline}%
    \hspace{1em}%
    \insertframenumber/\inserttotalframenumber
}

% Thème
\usetheme{Darmstadt}
\usecolortheme{seahorse}
\usepackage[T1]{fontenc}

%% La langue
\usepackage[english]{babel} 

% Math
\usepackage{bbm}
\usepackage{amsthm}
\usepackage{amsmath}
\usepackage{stmaryrd}
\usepackage{amssymb}
\usepackage{proof}
\usepackage{tikz}
\usepackage{booktabs}
\usetikzlibrary{positioning}
\newcommand{\mcl}{\mathcal{L}}
\newcommand{\mcd}{\mathcal{D}}
\newcommand{\mcc}{\mathcal{C}}
\newcommand{\ra}{\rightarrow}
\newcommand{\rsa}{\rightsquigarrow}
\newcommand{\llb}{\llbracket}
\newcommand{\rrb}{\rrbracket}
\newcommand{\R}{\mathbb{R}}
\newcommand{\C}{\mathbb{C}}
\newcommand{\K}{\mathbb{K}}
\renewcommand{\S}{\mathbb{S}}
\newcommand{\tend}[1]{\underset{#1\to +\infty}{\longrightarrow}}

%inference
\usepackage{semantic}

% colors
\usepackage{xcolor}

% barrer
 \usepackage{soul}

% wrap images around text
\usepackage{wrapfig}

% listing (code)
\usepackage{listings}
\lstset{
 language=caml,
 columns=[c]fixed,
 basicstyle=\small\ttfamily,
 keywordstyle=\color{blue},
 commentstyle=\color{Green}\bfseries,
 breaklines=true,
 mathescape=true
}

\newcommand{\etat}[1]{\langle #1 \rangle}
\newcommand{\etatz}[1]{\langle #1 \rangle_0}
\newcommand{\etatu}[1]{\langle #1 \rangle_1}
\newcommand{\etatd}[1]{\langle #1 \rangle_2}
\newcommand{\etatt}[1]{\langle #1 \rangle_3}

\definecolor{blue2}{rgb}{0.7, 0.83, 1}
\definecolor{orange}{rgb}{1, 0.83, 0.7}
\definecolor{grey}{RGB}{200,200,200}
\definecolor{link_color}{RGB}{0,0,200}

% \setbeamercolor{palette primary}{bg=Tan!30,fg=black}
% \setbeamercolor{palette secondary}{bg=Tan!70,fg=black}
% \setbeamercolor{palette tertiary}{bg=Tan!70,fg=black}
% \setbeamercolor{palette quaternary}{bg=Tan!10,fg=black}

% % 1- Block title (background and text)
% \setbeamercolor{block title}{bg=Bittersweet!50,fg=black}
% % 2- Block body (background)
% \setbeamercolor{block body}{bg=Bittersweet!10}

% % 1- Block title (background and text)
% \setbeamercolor{block title alerted}{bg=RedViolet!70,fg=black}
% % 2- Block body (background)
% \setbeamercolor{block body alerted}{bg=RedViolet!10}

% % 1- Block title (background and text)
% \setbeamercolor{block title example}{bg=ForestGreen!70,fg=black}
% % 2- Block body (background)
% \setbeamercolor{block body example}{bg=ForestGreen!10}


% Titre
\title{Two models, two measures}
\author{Ewen DUFOUR}
\institute{Université Paris-Saclay\\
Laboratoire Méthodes Formelles}
\date{16 juillet 2025}
\AtBeginSection[]
{ \begin{frame}
    \frametitle{Outline}
    \tableofcontents[currentsection]
  \end{frame} }

% Document
\begin{document}
\frame{\titlepage}

\section{Introduction}
\begin{frame}{Internship environment.}
\end{frame}
\subsection*{Problem presentation}
\begin{frame}{Invariance thesis.}
  \begin{itemize}
    \item Presented by Slot and Van Emde Boas.
    \item Existence of a computation model class.
    \item Contains Turing machines, RAM, etc.
    \item Simulate each other with:
     \begin{itemize}
        \item Polynomial overhead in time.
        \item Linear overhead in space.
      \end{itemize}
  \end{itemize}
\end{frame}
\begin{frame}{$\lambda$-calculus complexity.}
  What about $\lambda$-calculus?
  \begin{itemize}
    \item Time:
      \pause
      \begin{itemize}
        \item Weak $\lambda$-calculus.\checkmark
          \pause
        \item Strong $\lambda$-calculus.\checkmark
      \end{itemize}
      \pause
    \item Space:
      \begin{itemize}
        \item Only partial solutions.
      \end{itemize}
  \end{itemize}
\end{frame}
\begin{frame}{Linear substitution calculus.}  
\begin{itemize}
  \item $t ::=~x~|~\lambda x.t~|~t~t~|~t[x/t]$
\item  $\mcl ::= \square~|~\mcl[x/t]$
  \item $\mcc ::= \square~|~\mcc[x/t]~|~t[x/\mcc]~|~\mcc~t~|~t~\mcc~|~\lambda x.\mcc$
  \item Reduction rules:
    \begin{itemize}
      \item $ \mcl[\lambda x.t]~u~\ra_{d\beta}~\mcl[t[x/u]] $
      \item $\mcc\llb x\rrb[x/u]~\ra_{ls}~\mcc\llb u\rrb[x/u]$
      \item  $t[x/u]~\ra_{gc}~t \quad x\notin fv(t)$
    \end{itemize}
\end{itemize}
\end{frame}
\begin{frame}{Attack plan.}
  \begin{center}
\begin{tikzpicture}[auto,
        every path/.style ={draw, ultra thick, ->, % -Stealth[],
                            shorten >=1pt, line width=1pt},
        every node/.style ={thick, align=center, rounded corners, rectangle, minimum height=3.5em, minimum width=5em},
        WorkedOn/.style  ={ draw=black,  fill=blue2!70},
    ]

% Nodes

\node (b) [WorkedOn] {FLAME};
\node (a) [WorkedOn, left=of b] {LS(G)C};
\node (c) [WorkedOn,right=of b] {TM};


% Arrows
\draw (a) -- (b);
\draw (b) -- (c);

\end{tikzpicture}
\end{center}
\end{frame}
\section{Definitions}
\subsection*{LS(G)C}
\begin{frame}{Terms and strategy.} 
  \pause
  \begin{block}{Values}
  We distinguish as values the terms given by the grammar: $v ::= \lambda x.t~|~v[x/t]$
  \end{block}
  \pause
  \begin{block}{Evaluation contexts}
  For our strategy, we will use the contexts given by the grammar: $\mcc ::= \square~|~\mcc[x/t]~|~\mcc~t~|~v~\mcc$  
\end{block}
\begin{block}{Remark}
 We are no more substituting under abstraction or explicit substitutions. 
\end{block}
\end{frame}
\begin{frame}{Terms and strategy.}
    \begin{block}{Reduction strategy} 
    \begin{align*}
    &\infer[LA]{t~u\rightarrow t'~u}{t \rightarrow t'}
 &\infer[RA]{v~t\rightarrow v~t'}{t \rightarrow t'}\\
&\infer[GC]{t[x/u]\rightarrow t'}{t \rightarrow t' & x \notin fv(t') }
&\infer[US]{t[x/u]\rightarrow t'[x/u]}{t \rightarrow t' & x \in fv(t')}\\
&\infer[LS]{\mathcal{C}\llbracket x \rrbracket [x/u]\rightarrow \mathcal{C}\llbracket u \rrbracket [x/u]}{x \in fv(\mathcal{C}\llbracket u \rrbracket) }
&\infer[LSGC]{\mathcal{C}\llbracket x \rrbracket [x/u]\rightarrow \mathcal{C}\llbracket u \rrbracket}{x \notin fv(\mathcal{C}\llbracket u \rrbracket) }\\
&\infer[DB]{\mathcal{L}[\lambda x.t]~v \rightarrow \mathcal{L}[t[x/v]]}{x\in fv(t)}
&\infer[DBGC]{\mathcal{L}[\lambda x.t]~v \rightarrow \mathcal{L}[t]}{x\notin fv(t)}
\end{align*}
  \end{block}
\end{frame}


\begin{frame}{Space cost.}
  \begin{alertblock}{Space function for terms}
    We define the space function $|~|$ on terms as following:
  \begin{align*}
    |x| &= |dB(x)|\\
    |\lambda x.t| &= 1 + |t|\\
    |t~u| &= |t|+|u|\\
    |t[x/u]| &= |t|+|u|
  \end{align*}
  We extend this definition to contexts.
  \end{alertblock}
  \begin{block}{Remark}
    We assume that the actual terms are represented with de Bruijn indices.
  \end{block}
\end{frame}

\subsection*{FLAME}
\begin{frame}{FLAME.}
\begin{itemize}
    \item Decompose the term.
    \item Do the reduction.
    \item Rebuild the term.
      \item Do all again until it tries to reduce a value.
  \end{itemize} 
\end{frame}
\begin{frame}{Transitions.}
\begin{block}{FLAME transitions}
\label{FLAME}
\centering
\resizebox{\textwidth}{!}{%
\begin{tabular}{@{}l l l l l l l l l l@{}}
\toprule
\textbf{arg} & \textbf{term} & \textbf{context} & \textbf{state} & \textbf{transition}& \textbf{arg} & \textbf{term} & \textbf{context} & \textbf{state} \\
\midrule

– & $t[ u ] $ & $\mcc$ & 0 & $\rsa_{us}$ & – & $t$ & $\textsf{LS}(u) :: \mcc$ & 0  \\

- & $t~u$ & $\mcc$ & 0 & $\rsa_{lapp}$ & – & $t$ & $\textsf{RA}(u) ::\mcc$ & 0  \\
- & $v~t$ & $\mcc$ & 0 & $\rsa_{rapp}$ & – & $t$ & $\textsf{LA}(v) :: \mcc$ & 0  \\
- & $n$  & $\mcc$ & 0 & $\rsa_{ls}$ & - & $\uparrow^{n+1} t$ & $\mcc$ & 2 & $\textsf{LS}(t)=\textsf{nth\_subst}(\mcc)$ \\
- & $v_1~v_2$  & $\mcc$ & 0 & $\rsa_{db}$ & $(v_2,0)$ & $v_1$ & $\mcc$ & 1  \\

$(v_2, n)$ & $v_1[ t ]$ &$\mcc$ & 1 & $\rsa_{rdb}$ & $(v_2, n+1)$ & $v_1$ & $\textsf{LS}(t) :: \mcc$ & 1\\ 
$(v, n)$ & $\lambda.t $ &$\mcc$ & 1 & $\rsa_{fdb}$ & - & $t$ & $\textsf{LS}(\uparrow^{n}v) :: \mcc$ & 2 & $0\in fv(t)$\\
$(v, n)$ & $\lambda.t $ &$\mcc$ & 1 & $\rsa_{fdbgc}$ & - & $\downarrow t$ & $\mcc$ & 2& $0\notin fv(t)$\\
-&$v$ & $[]$ & 2 & $\rsa_{stop}$ & -& $v$ &$[]$ & 3 \\
-&$t$ & $[]$ & 2 & $\rsa_{dive}$ & -& $t$ &$[]$ & 0 \\
-&$t$ & $\textsf{LS}(u) :: \mcc$ & 2 & $\rsa_{gc}$ & -& $\downarrow t$ &$\mcc$ & 2 & $0\notin \textsf{fv}(t)$ \\
-&$t$ & $\textsf{LS}(u) :: \mcc$ & 2 & $\rsa_{bsubst}$ & -& $t[ u ]$ &$\mcc$ & 2 & $0\in \textsf{fv}(t)$ \\
-&$t$ & $\textsf{RA}(u) :: \mcc$ & 2 & $\rsa_{blapp}$ & -& $t~u$ &$\mcc$ & 2 \\
-&$t$ & $\textsf{LA}(u) :: \mcc$ & 2 & $\rsa_{brapp}$ & -& $u~t$ &$\mcc$ & 2 \\

% \bottomrule
\end{tabular}
}
% \caption{FLAME transitions.}
\end{block}
\end{frame}
\begin{frame}{Cost model.}
  \begin{block}{Notation}
    An actual state of the machine is given by $\etat{(v,n),t,\mcc}_e$. %where
    % \begin{itemize}
    %   \item $t$ is the term we are evaluating.
    %   \item $\mcc$ is a pile of contexts.
    %   \item $(v,n)$ is a possibly empty couple where $v$ is the argument of an application and $n$ is the distance to the abstraction;
    % \end{itemize}
  \end{block}
  \begin{alertblock}{Space function for states.}
  We define the space function $|~|$ on states as following:
  \begin{align*}
    |\etat{(v, n),t,\mcc}_e| = |v|+|n|+|t|+|\mcc|
  \end{align*}
  \end{alertblock}
  \begin{block}{Remarks.}
   We are not counting the size of $e$ because it's constant. Moreover, when $(v,n)$ is empty ($-$), we set their size to 0. 
  \end{block}
\end{frame}

\section{Simulation of LS(G)C}
\subsection*{Clean property}
\begin{frame}{Clean function.}
  
\end{frame}
\begin{frame}{Base case: DB.}
\end{frame}
\begin{frame}{Base case: DB.}
\end{frame}
\begin{frame}{Inductive case: GC.}
\end{frame}

\subsection*{FLAME behaviour}
\begin{frame}{Important lemmas.}
  
\end{frame}

 \subsection*{Simulation}
 \begin{frame}{Theorems.}  
 \end{frame}
 \begin{frame}{Base case: DB.}
   
 \end{frame}
 \begin{frame}{Inductive case: GC.}
   
 \end{frame}

\section{Simulation of FLAME}
\begin{frame}{Needs.}
  
\end{frame}
\begin{frame}{Encoding.}
\end{frame}
\begin{frame}{Internal functions of FLAME.}
  
\end{frame}
\begin{frame}{Theorem.}
\end{frame}

\section{Conclusion}
\begin{frame}{Recap.}
\end{frame}
% \subsection*{Internship}
% \begin{frame}{Internship Environnement}
%
% \end{frame}
%
%
% \begin{frame}{Initial Goal}
%     \begin{alertblock}{Initial goal}
%         Characterize time complexity classes using FaustML 
%     \end{alertblock}\pause
%     \begin{block}{Definition: FaustML}
%         A function in FaustML follows the following grammar: 
%     \begin{align*}
%         \text{ Function } f(x_1,..,x_n) &= e \\
%         \text{ Expression } \qquad \quad e ::&= x \\
%         & |\; let \; x  =  e_1 \; in \; e_2 \\
%         & |\; c(e_1,..,e_n) \\
%         & |\; f(e_1,..,e_n) \\
%         & |\; match \; e \; with \; (c_i(x_1,..,x_n) \to e_i)_i \\
%     \end{align*}
%     \end{block}
% \end{frame}
%
% \subsection*{history of ICC}
%
% \begin{frame}{ICC's dream}
%     \small ICC = Implicit Computational Complexity\\
%
%     % \includegraphics[width= 0.9\textwidth]{mygcc.png}
% \end{frame}
%
% \begin{frame}{ICC "rough" picture}
%
%     % \includegraphics[width=0.9\textwidth]{ICC-full-picture.png}
% \end{frame}
%
% \begin{frame}[fragile]{THE ICC's Question}
%     \centering \textbf{what is wrong with this \st{picture} program?} 
%
%     \begin{lstlisting}
%     let rec twice(n) = match n with 
%         | Z $\to$ Z 
%         | S(m) $\to$ S(S(twice(m)))
%     let rec exp(n) = match n with 
%         | Z $\to$ S(Z)
%         | S(m) $\to$ twice(exp(m))
%     \end{lstlisting}
% \end{frame}
%
% \begin{frame}[fragile]{THE ICC's Question}
%     \centering \textbf{what is wrong with this \st{picture} program?} 
%     \begin{lstlisting}
%     let rec twice(n) = match n with 
%         | Z $\to$ Z 
%         | S(m) $\to$ S(S(twice(m)))
%     let rec exp(n) = match n with 
%         | Z $\to$ S(Z)
%         | S(m) $\to$ twice(exp(m))
%     \end{lstlisting}
%     In the data-tiering branch, the \textbf{composition of twice and exp} is the problem.
% \end{frame}
%
% \section{data-tiering}
% \begin{frame}{Data-tiering}
%     \begin{block}{tiering}
%         data-tiering is affecting to each expressions and functions integers (called \textbf{tier}) and add constraints to them.
%         It is stated as a \textbf{typing} judgment $f \triangleright i_1,..,i_n \to k$
%     \end{block} \pause
%     for example the composition propagate the tiers:
%     \begin{gather*}
%         \inference{f \triangleright p \to k \quad g \triangleright i \to p}{f \circ g \triangleright i \to k}
%     \qquad \inference{f \text{ recursive } &...& \color{red}{p > k}}{f \triangleright p \to k}
%     \end{gather*}
% \end{frame}
%
% % \begin{frame}[fragile]{Data-tiering's problem}
% %     Data-tiering is unable to accept the usual implementation of insert sort, because it have the same constraint problem
% %     \begin{lstlisting}
% %     (*insert: i,j->j with i>j*)
% %     let rec insert(l,x) = ...
% %     (*sort: p->k ERROR *)
% %     let rec sort(l) = match l with 
% %         | Nil $\to$ Nil 
% %         | Cons(x,l) $\to$ insert(sort(l),x) 
% %     \end{lstlisting}
% % \end{frame}
%
% \begin{frame}{Data-tiering's problem}
%     % \includegraphics[scale=0.1]{same-picture-ICC.png}
% \end{frame}
%
% \begin{frame}[fragile]{Data-tiering solution?}
% If we use a fuel, we can bypass this problem:
% \begin{lstlisting}
% let rec insert(n,l,x) = ... (*n is a fuel*)
%
% let rec sort(n,l) = match n with 
%   | Z $\to$ l
%   | S(m) $\to$ match l with 
%     | Nil $\to$ Nil 
%     | Cons(x,l1) $\to$ insert(m,sort(m,tl(l)),x)
%               (* require parameter subst *)
% \end{lstlisting}
%
% But now we need our characterization to allow parameter substitution, 
% which was not the case of SIMREC from Avanzini and Dal Lago 
%
% \end{frame}
%
% \section{SIMREC parameter subst}
%
% \begin{frame}{Extension to SIMREC}
%     SIMREC structure (from Avanzini and Dal Lago):
% \begin{align*}
%     f &:= f_c & f() &= c() \label{null-constr0} \tag{0-constr}\\
%     f &:= f_c &f(\vec{x}) &= c(\vec{x}) \label{constr0} \tag{constr}\\
%     f &:= \Pi_i^n\;\;& f(x_1,..,x_n) &= x_i \label{proj0} \tag{proj}\\
%     f &:= h \circ (g_1,..,g_k) &f(\vec{x}) &= h(g_1(\vec{x}),..,g_k(\vec{x})) \label{comp0} \tag{comp}\\
%     f &:= case((g_i)_i)& f(c_i(\vec{x}), \vec{y}) &=  g_i(\vec{x}, \vec{y}) \label{case0} \tag{case}  \\
%     f_j &:= simrec((h_{ij})_{ij})_j & f_j(c_i(\vec{x}), \vec{y}) &=  h_{ij}(\vec{x}, \vec{y}, f_1(x_1, \vec{y})..f_1(x_n, \vec{y}).. f_k(x_n, \vec{y})) \label{rec0} \tag{rec}
% \end{align*}\pause
%     \begin{block}{Definition}
%         The class \textbf{SAFE} is the smallest class of function containing (0-constr) (proj) and closed under (comp) and (case) 
%     \end{block}
% \end{frame}
% \begin{frame}[fragile]{Safe functions}
%     \begin{example}
%         \texttt{tl} is a SAFE function:
%     \begin{lstlisting}
%         tl(l) = match l with 
%             | Nil $\to$ Nil 
%             | Cons(x,l) $\to$ l
%     \end{lstlisting}
%     \end{example}    \pause
%
%     \begin{alertblock}{safe function's crutial property}
%         if $\pi \in SAFE$ then for any values $\vec{v}$: $STs(\pi(\vec{v})) \subseteq STs(\vec{v}) \cup E$
%     \end{alertblock}
%     \small STs(v) are the sub-terms of v
% \end{frame}
% \begin{frame}{PSIMREC}
% \begin{align*}
%     f &:= f_c &f(\vec{x}) &= c(\vec{x}) \tag{constr}\\
%     f &:= \Pi_i^n\;\;& f(x_1,..,x_n) &= x_i  \tag{proj}\\
%     f &:= h \circ (g_1,..,g_k) &f(\vec{x}) &= h(g_1(\vec{x}),..,g_k(\vec{x})) \tag{comp}\\
%     f &:= case((g_i)_i)& f(c_i(\vec{x}), \vec{y}) &=  g_i(\vec{x}, \vec{y}) \tag{case}  
% \end{align*}
% \begin{gather*}
%     f_j := psimrec((h_{ij})_{ij}, (\vec{\pi}_{ijklzw})_{ijklzw})_j  \tag{p-rec}
% \end{gather*}
%     \begin{block}{Definition}
%         The class \textbf{PSIMREC} is the smallest class of function containing (constr) (proj) and closed under (comp), (case) and (p-rec)
%     \end{block}
% \end{frame}
% \begin{frame}{PSIMREC vs SIMREC}
%     $\pi_i$ are SAFE functions
% \begin{align*}
%     f_i(nil,x,y) &= g_i(x,y) \tag{p-rec}\\
%     f_i(bin(z,w),x,y) &= h_i(z,w,x,y,f_1(z,\pi_1(x), \pi_2(y)),f_1(w,\pi_3(x), \pi_4(y)), \\ & \qquad \qquad \qquad \; \;f_1(w,\pi_5(x), \pi_6(y)),f_2(w,\pi_7(x), \pi_8(y)))\\
%     \text{-} \\
%     f_i(nil,x,y) &= g_i(x,y)\tag{rec}\\
%     f_i(bin(z,w),x,y) &= h_i(z,w,x,y,f_1(z,x, y),f_1(w,x, y), \\ & \qquad \qquad \qquad \; \;f_2(z,x, y),f_2(w,x, y))
% \end{align*}
% \end{frame}
% \begin{frame}{PSIMREC Soundness}
%     \begin{alertblock}{PSIMREC Soundness}
%         For every function $f \in PSIMREC(\mathbb{A})$, with $f \triangleright i_1,..,i_n \to k$.\\
%         For all inputs $\vec{v}$
%         a DAG representation of $f(\vec{v})$ is \textbf{computable in time polynomial} in $\|\vec{v}\|$
%     \end{alertblock}\pause
%     % \includegraphics[width=\textwidth]{DAG-tree.png}\tiny *
%     *Figure from \textit{Avanzini and Dal Lago "On sharing, Memoization and Polytime" (2018)}
% \end{frame}
%
% \begin{frame}{PSIMREC proof}
%     For every function $f \in PSIMREC(\mathbb{A})$:
%     \begin{block}{poly space}
%         it exists $p_f$ such that $\|f(\vec{v})\| \leq p_f(\|\vec{v}\|)$
%     \end{block}
%     \begin{block}<2->{function call time}
%         $(C,f(\vec{v})) \Downarrow_m (D,u)$ with $m=|FC^f_{\vec{v}} \setminus C|$
%     \end{block}
%     \begin{block}<3->{function call uper bound}
%         it exists $q_f$ such that $|FC^f_{\vec{v}}| \leq q_f(\|\vec{v}\|)$ 
%     \end{block}
%     \small *  $\|\vec{v}\|$ is the maximally shared size of $\vec{v}$ \\
%     **$FC^f_{\vec{v}}$ are the function call arising from $f(\vec{v})$
% \end{frame}
%
%
% \section{FaustML to PSIMREC}
% % \begin{frame}{FaustML transformations}
% %     \begin{center}
% %         \includegraphics[width=0.9\textwidth]{repport-diagram.excalidraw.png}
% %     \end{center}
% % \end{frame}
%
% \begin{frame}{FaustML Soundness}
%     \begin{alertblock}{FaustML Soundness}
%     For every function $f$ in any version of FaustML above, with $f \triangleright i_1,..,i_n \to k$.\\
%         For all inputs $\vec{v}$
%         a DAG representation of $f(\vec{v})$ is \textbf{computable in time polynomial} in $\|\vec{v}\|$
%      \end{alertblock}\pause
%     % \includegraphics[width=\textwidth]{DAG-tree.png}\tiny *
%     *Figure from \textit{Avanzini and Dal Lago "On sharing, Memoization and Polytime" (2018)}
% \end{frame}
%
% \begin{frame}{FaustML proof}
%     $\beta : $ FaustML nested function $\to$ FaustML version of PSIMREC
%     \begin{block}{Tiering preservation}
%         if $f \triangleright J \to k$ then $\beta(f) \triangleright J \to k$
%     \end{block}\pause
%
%     \begin{block}{value correctness}
%         if $\emptyset \vdash f(\vec{v}) \downarrow u$ then $\emptyset \vdash \beta(f)(\vec{v}) \downarrow u$
%     \end{block} \pause
%
%     \begin{block}{time correctness}
%         $FC^{\beta(f)}_{\vec{v}} \subseteq FC^{f}_{\vec{v}}$
%     \end{block}
% \end{frame}
%
% \section*{Conclusion}
% \begin{frame}{Conclusion}
% \begin{block}{PSIMREC}
%     We extended SIMREC with parameter substitution, which has practical interest in term of expressivity (ex: insert-sort)
% \end{block}\pause
%
% \begin{block}{FaustML}
%     We carried out the previous result in the much nicer syntax of FaustML, which makes it easier to use 
% \end{block}\pause
%
% \begin{block}{Implementation}
%     We implemented it in OCaml which gives an idea of how we could use this result in a compiler
% \end{block}
% \end{frame}
%
% \begin{frame}{Thanks}
%     \begin{center}
%         % \includegraphics[width=0.9\textwidth]{repport-diagram.excalidraw.png}
%     \end{center}
% \end{frame}

\end{document}
